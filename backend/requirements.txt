fastapi>=0.115,<0.116
uvicorn[standard]>=0.30
pydantic>=2.0

# LLM 调用（F-BK-DT-03 / F-LM-*）
openai>=1.30
python-dotenv>=1.0

# SQL 血缘 parser（F-BK-02）
sqlglot>=25.0

# Neo4j 推送（F-EX-07）
neo4j>=5.20,<6

# BI 报表数据处理（F-DS-01~07）
pandas>=2.0
openpyxl>=3.1
pyarrow>=15.0  # parquet 落盘；未安装时 bi_service 自动回退 pickle

# 测试
pytest>=8.0
httpx>=0.27
